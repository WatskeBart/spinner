# Spinner for commands

Show a spinner during a running command.

## Build

```bash
git clone https://github.com/WatskeBart/spinner.git
cd spinner
go build -o spinner spinner.go
```

Example: `./spinner sleep 5`